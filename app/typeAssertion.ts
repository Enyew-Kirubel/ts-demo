
const stringMethod = 'GET'
const requwstob = { url: "https://example.com", method: "GET" };
const req = (Method: 'GET' | 'POST') => {
    // some action
}
req(requwstob.url as "GET")
req(stringMethod)

